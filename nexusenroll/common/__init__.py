"""
nexusenroll.common

The shared kernel used by all three service modules (student, faculty,
admin): common domain entities, CSV-backed repositories, the
Enterprise Service Bus, and the notification contract. See
domain.py and repositories.py for the design rationale.
"""
