"""
nexusenroll

The NexusEnroll university course-enrolment system: one Python package
containing all three business modules (student, faculty, admin), the
shared kernel they're built on (common), and the integration layer
that wires them together (system).

Run the whole system with:  python main.py   (from the repository root)
"""
